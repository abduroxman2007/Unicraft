[build]
builder = "NIXPACKS"

[deploy]
releaseCommand = "python manage.py collectstatic --noinput && python manage.py migrate"
startCommand = "gunicorn unimentor.wsgi --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/schema/"
healthcheckTimeout = 120
